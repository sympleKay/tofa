import {
  PORT,
  DB_HOST,
  DB_NAME,
  DB_PASSWORD,
  DB_PROTOCOL,
  DB_USER,
  URL_ENDPOINT,
} from "./env.until";
import { photoGallery } from "./data.util";
import { DBCONNECTION } from "./DBConn.util";

export {
  PORT,
  DB_HOST,
  DB_NAME,
  DB_PASSWORD,
  DB_PROTOCOL,
  DB_USER,
  URL_ENDPOINT,
  DBCONNECTION,
  photoGallery,
};
